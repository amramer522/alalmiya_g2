import 'package:flutter/material.dart';

class FavsPage extends StatelessWidget {
  const FavsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("FavsPage")),
    );
  }
}
